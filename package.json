{
  "name": "lucid-engine",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "playground": "vite --config playground/vite.config.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.0.1"
  }
}
